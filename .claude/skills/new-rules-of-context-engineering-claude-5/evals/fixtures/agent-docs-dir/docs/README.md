# Docs

Documentation for the example project.

- `agents/` — instructions for our automation agents (deploy, release,
  support triage).
- `architecture.md` — system overview for new engineers.

To contribute to these docs, open a PR against `main`. The docs site is
rebuilt automatically on merge.
